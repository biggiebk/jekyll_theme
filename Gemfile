source 'https://rubygems.org'

gem 'jekyll', '~> 4.4'

# If you want to build with the GitHub Pages environment, you can use the
# `github-pages` gem instead of pinning Jekyll directly. GitHub Pages
# pins specific gem versions; uncomment the line below to match Pages:
#
# gem 'github-pages', group: :jekyll_plugins

# Add any other gems you need here
